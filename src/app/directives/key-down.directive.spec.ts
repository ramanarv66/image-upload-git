import { KeyDownDirective } from './key-down.directive';

describe('KeyDownDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyDownDirective();
    expect(directive).toBeTruthy();
  });
});
