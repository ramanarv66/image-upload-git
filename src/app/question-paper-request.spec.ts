import { QuestionPaperRequest } from './question-paper-request';

describe('QuestionPaperRequest', () => {
  it('should create an instance', () => {
    expect(new QuestionPaperRequest()).toBeTruthy();
  });
});
